[project]
name = "tsuchi"
version = "0.1.0"
description = "JavaScript AOT compiler via LLVM + QuickJS-NG"
requires-python = ">=3.12"
dependencies = ["llvmlite>=0.44", "rich>=13.0", "tree-sitter>=0.24", "tree-sitter-javascript>=0.23", "tree-sitter-typescript>=0.23"]

[project.scripts]
tsuchi = "tsuchi.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tsuchi"]

[tool.ux]
entry = "tsuchi"
include = [
    "vendor/quickjs-ng/quickjs.h",
    "vendor/quickjs-ng/quickjs-libc.h",
    "vendor/quickjs-ng/build/",
    "vendor/clay/",
    "vendor/termbox2/",
    "vendor/tsuchi_game_framework.c",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
